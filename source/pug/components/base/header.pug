mixin header(headerMods, currentUrl)
  header.header(class=headerMods)
    .container
      a.logo(href="index.html")
        span.visually-hidden Переход на главную
        +icon("logo", "332", "48").logo__icon
        +icon("logo", "48", "48").logo__icon-mob

      +btn-user({text: "Войти"})
